# Backdoor

This is a fully functional backdoor which is persistent and works regardless of pyhton or OS difference (Not my original project)
For it to work :
1. The IP's must be specified of the attacker in both the files
2. The attacker must run listener.py on his machine
3. Better to package the backdoor.py using pyinstaller command in comment
4. Target must open the pdf and automatically the connection is established
